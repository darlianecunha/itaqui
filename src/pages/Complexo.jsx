import React,{useEffect,useMemo,useState} from 'react';
import {ResponsiveContainer,LineChart,Line,XAxis,YAxis,CartesianGrid,Tooltip,Legend} from 'recharts';
import {TrendingUp,Factory} from 'lucide-react';
import {loadCSV} from '../util';
const Card=({children})=>(<div className='card'>{children}</div>);
const Title=({title,subtitle})=>(<div style={{marginBottom:'.5rem'}}><h3 style={{fontSize:'1.125rem',fontWeight:600}}>{title}</h3>{subtitle&&<p className='subtitle'>{subtitle}</p>}</div>);
export default function Complexo(){const [dados,setDados]=useState([]);useEffect(()=>{loadCSV('/data/complexo.csv').then(setDados)},[]);const totais=[...dados].map(r=>({ano:r.ano,Itaqui:r.itaqui,'Ponta da Madeira':r.madeira,'ALUMAR/ALCOA':r.alumar,total:r.itaqui+r.madeira+r.alumar})).sort((a,b)=>a.ano-b.ano);const cards=[...totais].sort((a,b)=>b.ano-a.ano);return(<div className='grid'><div className='grid g2'>{cards.map(r=>(<Card key={r.ano}><Title title='Total do Complexo' subtitle={`${r.ano} (t)`}/><div className='kpi'>{Number(r.total).toLocaleString('pt-BR')}</div></Card>))}</div><Card><Title title='Porto do Itaqui — Série Anual (t)' subtitle='Valores absolutos'/><div style={{height:'320px'}}><ResponsiveContainer width='100%' height='100%'><LineChart data={totais}><CartesianGrid strokeDasharray='3 3'/><XAxis dataKey='ano'/><YAxis/><Tooltip formatter={v=>typeof v==='number'?v.toLocaleString('pt-BR'):v}/><Legend/><Line type='monotone' dataKey='Itaqui' strokeWidth={3} dot={{r:3}}/></LineChart></ResponsiveContainer></div></Card><Card><Title title='Ponta da Madeira — Série Anual (t)' subtitle='Valores absolutos'/><div style={{height:'320px'}}><ResponsiveContainer width='100%' height='100%'><LineChart data={totais}><CartesianGrid strokeDasharray='3 3'/><XAxis dataKey='ano'/><YAxis/><Tooltip formatter={v=>typeof v==='number'?v.toLocaleString('pt-BR'):v}/><Legend/><Line type='monotone' dataKey='Ponta da Madeira' strokeWidth={3} dot={{r:3}}/></LineChart></ResponsiveContainer></div></Card><Card><Title title='ALUMAR/ALCOA — Série Anual (t)' subtitle='Valores absolutos'/><div style={{height:'320px'}}><ResponsiveContainer width='100%' height='100%'><LineChart data={totais}><CartesianGrid strokeDasharray='3 3'/><XAxis dataKey='ano'/><YAxis/><Tooltip formatter={v=>typeof v==='number'?v.toLocaleString('pt-BR'):v}/><Legend/><Line type='monotone' dataKey='ALUMAR/ALCOA' strokeWidth={3} dot={{r:3}}/></LineChart></ResponsiveContainer></div></Card></div>)}
